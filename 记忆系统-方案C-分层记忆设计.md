# 记忆系统方案 C（暂缓）

状态：暂不属于当前版本，本文保留为历史设计记录。

## 当前事实

当前小未来只保留两种与聊天有关的状态：

1. `src/engine/generic.js` 中进程内最近 12 轮上下文，用于当前运行期间的连续对话。
2. `src/services/chat-history.js` 持久化用户和 assistant 消息，用于聊天记录窗口展示。

聊天记录不会自动变成长程记忆，也不会通过关键词、Judge 或反思服务注入 system prompt。应用重启后可以继续查看聊天记录，但模型上下文从空状态重新开始。

## 暂缓原因

当前产品已经主动收窄为“纯 Live2D + 核心聊天 + 人格 + Provider”。长期记忆会引入新的隐私、删除、迁移、提示词预算和测试边界，在没有明确产品需求前不应恢复。

以下旧模块已经从代码中删除，文档中的旧实现描述不再适用：

- `memory-service.js`、`memory-store.js`
- `memory-intent.js`、`memory-judge.js`、`memory-reflection.js`
- owner、日程、主动策略和设置存储模块

## 未来重新启用时的最低要求

如果未来重新实现长期记忆，必须先明确：

- 用户可见的开启/关闭开关和数据范围。
- 单条查看、删除、全部清空和导出能力。
- 敏感信息默认不记忆，且不会出现在普通日志。
- 记忆与聊天记录分离，不能把全部历史直接塞进 prompt。
- 迁移、过期、冲突和错误恢复策略。
- 独立测试，不影响核心聊天和 Live2D 交互。

在这些条件满足前，`chat-history.json` 只能作为可回看的聊天记录，不应被称为“长期记忆”。
