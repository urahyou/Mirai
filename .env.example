# Copy this file to .env and add only the keys required by your Providers.
# .env is ignored by Git and must never be committed.

MIRAI_PROVIDER_DEEPSEEK_API_KEY=
MIRAI_PROVIDER_OLLAMA_MAC_API_KEY=

# TiMem 长期记忆（默认关闭；密钥/密码只放在本机 .env，不要提交）
TIMEM_ENABLED=false
TIMEM_BASE_URL=https://api.timem.cloud
TIMEM_API_KEY=
# 也可以不用 API Key，改用账号密码登录
TIMEM_USERNAME=
TIMEM_PASSWORD=
TIMEM_USER_ID=mirai-owner
TIMEM_CHARACTER_ID=mirai
TIMEM_SESSION_ID=desktop-session
