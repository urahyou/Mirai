# 小未来 · UI 与体验重新设计方案（V2）

- 版本：0.1（Draft）
- 背景：项目已引入**分层记忆系统（方案 C）**——core 常驻画像 / archival 活跃档案 / working 动态工作记忆，外加自动 Memory Judge 沉淀（P2 已完成），未来还有反思压缩（P3）。现有 UI（线性右键菜单 + 平铺记忆列表）无法承载这套记忆模型，需整体重新梳理信息架构。
- 原则：**可解释、可撤销、本地可控**；不改变既有核心交互肌肉记忆（单击搭话 / 双击聊天 / 右键菜单）。

---

## 1. 现状问题盘点（对照代码）

| # | 问题 | 代码佐证 |
|---|------|----------|
| 1 | 右键菜单是**线性列表**，动作与导航混合、无分组，条目已到 11 项且将随记忆/反思增长 | `renderer/menu.js` `buildMenu()` |
| 2 | 记忆在设置面板里是**平铺列表**，只有类型筛选，完全无法表达 core/archival/working 分层、来源（自动/手动）、重要性/置信度/访问频率 | `renderer/settings-panel.js` `renderMemories()` |
| 3 | 「设置与记忆」一个面板塞了主动+提醒+记忆三块，职责混杂、层级不清 | `renderer/settings-panel.html` |
| 4 | **自动记忆（Judge）没有任何用户可见开关/频率/状态**，只有硬编码 60s 节流 | `main/main.js` `AUTO_MEMORY_MIN_INTERVAL_MS` 写死 |
| 5 | 聊天回复**不可解释**：用户不知道这次回答用了哪条记忆、core 注入了什么 | `generateReply` 注入后无任何展示 |
| 6 | 逐条记忆没有"不要记住/锁定/归档"这类精细控制，只有整条删除 | `settings-panel.js` |
| 7 | 反思压缩（P3）落地后，`isSummary`/`subEntryIds` 需要在 UI 有对应呈现和审计，现无 | `memory-store.js` 分层字段已预留 |

---

## 2. 信息架构（IA）重设计

```
右键菜单（分组导航）
 ├─ 角色     ：状态、小未来的性格、关于主人、重置位置、隐藏角色
 ├─ 记忆     ：【记忆管理中心】、清空记忆（危险）
 ├─ 能力     ：聊天、日程提醒、Provider
 └─ 系统     ：设置、退出
          │  （右键点开，意图 = 「看一下有什么 / 快速操作」）
          └───────────── 打开对应面板
```

**决策：面板从「一屏塞三块」改为「四个独立面板 + 一个顶部导航条」**，各自职责单一：
1. **记忆管理中心**（新增，核心）
2. **偏好设置**（拆分自原 settings，只留主动/提醒/传输）
3. **关于主人 / 性格 / Provider / 日程**（保持独立面板，微调）
4. 设置与记忆的窗口 → 复用为带导航条的单窗口多页

---

## 3. 主菜单重设计（`menu.html` / `menu.js`）

用**分组 + 分隔线 + 二级子菜单**取代扁平列表：

```
小未来
─────────────
▸ 角色
   查看状态
   小未来的性格
   关于主人
   重置位置
   隐藏角色
▸ 记忆
   ★ 记忆管理中心        ← 新增，第一优先
   ─────────
   （勾选项）自动沉淀记忆  ← 快速开关 Judge
   清空记忆（危险，二次确认）
▸ 能力
   开始聊天
   日程提醒
   Provider 设置
▸ 系统
   偏好设置
   退出（危险，二次确认）
```

- 「自动沉淀记忆」为**勾选型**项，直接读/写 `settings.memoryAuto`，免进面板即可开关。
- 状态区每项在数据就绪前显示 loading；Provider 子菜单保留现有二级展开。

---

## 4. 面板窗口结构重构（单窗口导航）

将「设置与记忆」窗口升级为**主导航容器**，顶部一个横排 Tab：

```
[ 记忆库 │ 主动与提醒 │ 聊天表现 │ 传输与隐私 ]
```

- 现有 `settings-panel.html/js/css` 拆为多 Tab，共用同一 `#app` 骨架。
- 每个 Tab 独立 section，切换只更新内容区，不重开窗口。
- **记忆单列一页「记忆库」**，不再和设置挤在一起。
- 带来收益：后续新增页（如反思压缩审计、每日总结）只需加一个 Tab，不用再加菜单项。

---

## 5. 记忆库（核心功能，取代原平铺记忆列表）

> 用户诉求核心：**要不要一个记忆库集中展示记忆、删掉不符合架构/无用的**。答案：要，且记忆库是记忆系统的第一入口。

### 5.1 分层视图（4 个 Tab）
```
[ 常驻画像 core ] [ 活跃记忆 archival ] [ 回收站(软删) ] [ 全部 ]
```

- **常驻画像**：`status=core`，重量级、常驻注入、不可轻易删（删除需二次确认）。
- **活跃记忆**：`status=active/aging` + 未归档，承载自动/手动沉淀主体，默认按重要性排序。
- **回收站**：软删除的记忆（`deletedAt`），可**还原**或**彻底删除**。
- **全部**：含已归档 `archived`、回收站、压缩摘要 `isSummary`（P3 起），并可在此做来源/类型筛选。

### 5.2 记忆条目卡片字段
每条记忆展示：
```
[类型徽章] 内容摘要……
  重要性 ●●○○  置信度 ●●●○  访问 ×N  ·  来源[自动|手动|旧]  ·  层[常驻|活跃]
  （摘要时）包含子条目 N 条 → 展开查看 subEntryIds
  [编辑] [设为常驻/降级] [归档/还原] [不要记住] [删除✕→回收站]
```
- **来源徽章**：`自动`（Judge 沉淀）、`手动`（用户 `记住...` 命令）、旧 `conversation` 标记「**旧数据**」引导审视。
- **重要性/置信度**：用圆点/条可视化，不需数字但 tooltip 带数值。
- **操作细化**：升 core / 降级 / 归档 / 还原 / 锁定（不要记住同类）；删除统一先进回收站。
- **排序**：默认按「最近访问 × 重要性」综合权重降序，旧而无用的自然沉底。

### 5.3 记忆卫生（memory hygiene）：清理机制（核心新增）

记忆库顶部一个「**清理建议**」按钮，展开系统扫描出的“值得处理”记忆，可逐条/一键勾选归档或删除：

| 标签 | 判定条件 | 默认处理 |
|------|----------|----------|
| ⏰ 已过期 | `expiresAt` 已过（如一次性日程） | 自动归档 |
| 🪤 低价值 | `importance`<0.25 或 `confidence`<0.5，且 `accessCount=0`、距今远 | 建议归档 |
| 🕰 长期未用 | `accessCount=0` 且创建超 N 天、无重要性 | 建议归档 |
| 🔁 疑似重复 | `fingerprint(type, content)` 相同/近似 | 建议合并/删一 |
| ⚠ legacy 旧数据 | `source: conversation` 旧样式条目 | 提示审视/升级 |

- **绝不自动删除**：全部只“建议 + 归档”，删除必经用户确认或先进回收站。
- 对齐方案 C 保守原则：宁可保留，不误删。

### 5.4 软删除（回收站）

- 尊重已接受的 `memorySoftDelete`：删除 = 置 `deletedAt` → 进「回收站」，可**还原**。
- 回收站内再点「彻底删除」才真正 `erase`。
- 「清空记忆」仍为危险操作（软删后清空回收站）。

### 5.5 自动记忆设置 + Judge 审计（可解释性）
```
自动沉淀记忆        [开关]（对应 memoryAuto）
提炼频率            [每轮/每5分钟/每30分钟/每小时]（替代硬编码 60s）
提炼范围            [偏好/资料/事件/工作/日程]（来源类型白名单）
最近沉淀日志        （最近 N 条 "wrote ..." 时间+内容，来自 main.js 的 judge 记录）
```
- **Judge 日志可视化**：main 进程把每次 `wrote/skip` 写入内存环形队列，面板拉取展示，用户能看到"它记了什么、为什么没记"（可解释性）。

### 5.6 数据接入
- 复用现有 `desktopPet.memory.*`（list/update/archive/forget/doNotRemember/export/clearAll）。
- **新增 IPC**（`main.js` + `preload.js`）：
  - `memory:stats` → 各层/来源计数、清理建议清单、最近 judge 日志、自动记忆开关
  - 软删扩展：`list(includeTrash)`、`restore(id)`、`purge(id)`（基于 memory-service 的 `deletedAt` 模型）
  - `memory:updateMeta`（用现有 `update` 改 status/importance/confidence）
- **无需新依赖**，纯 JSON + 现有 service。

---

## 6. 可解释性：让「用到的记忆」可见

### 6.1 状态面板（渲染进程）增强
- 在 `renderStatePanel` 的状态卡里加一行：`本次对话注入记忆 core ×N / working ×M`（由 main 在构造 context 时计数并随 `state:changed` 带出）。

### 6.2 气泡/聊天输入框
- 聊天输入框底部一条极淡的提示条：`本回答参考了 3 条记忆（常驻 1 · 档案 2）`，点击展开明细。
- 避免打扰：默认只显示计数，不展开段落。

### 6.3 记忆卡片 tooltip
- 悬停记忆内容显示其**注入时的原文片段**（若来自 Judge，附原始对话摘录）。

---

## 7. 细节交互清单（统一规范）

- **危险动作统一**：清空记忆 / 退出 / 删除 core / 清空日程 → 统一"二次确认 + 3s 冷却"（现 menu 已有 mkDanger，抽取为公共能力复用到面板）。
- **空状态**：记忆为空时显示引导 `小未来会自然记住你的偏好～也可以手动输入「记住我喜欢喝美式」`。
- **反馈**：所有写操作后 toast 确认（复用 `onToast`）。
- **可撤销**：删除改为先归档（软删），面板内"回收站"可还原；只有"彻底清空"才真正 erase。
- **Locale**：全部中文文案，风格延续粉色系 `#ff4f9e`。

---

## 8. 落地步骤（分阶段，每步可独立验收）

| 阶段 | 内容 | 涉及文件 |
|------|------|----------|
| **U1 菜单分组** | 菜单改分组+子菜单+自动记忆勾选项，不改动逻辑 | `menu.html/js`、`preload.js` |
| **U2 偏好设置拆分** | settings 拆 Tab（记忆挪走→U3），剩主动/提醒/传输 | `settings-panel.*` |
| **U3 记忆库** | 分层 Tab + 卡片 + 操作 + 清理建议 + 自动记忆设置 + judge 日志 | **已完成**：`memory-panel.*`、`memory:stats/restore/purge` IPC、`preload`、菜单「记忆库」入口、`memory-service` 软删(`deletedAt`)/`restore`/`purge`/`hygiene`/`blocked`/`update(status)`、judge `onJudged` 回调 + 环形日志队列 |
| **U4 可解释性** | ✅ 已完成 | 状态卡记忆行（常驻/档案/摘要计数，点击开记忆库）、state:get 与 state:changed 均附 memoryCounts |
| U5 细节 | ✅ 已完成 | settings-panel 旧平铺记忆列表移除，改「打开记忆库」入口 + 分层概览；记忆库加「已压缩/摘要」徽章 |
| **U5 细节打磨** | 空状态、软删除、二次确认复用、toast | 各面板 |

> 依赖：U3 部分能力需 `memory-meta` IPC（改 status/importance），在 memory-service 现有 `update` 上扩展，属 P2 记忆系统的自然延伸，不影响既有测试契约。

---

## 9. 本次不包含（明确边界）

- 不做向量检索 / 图谱（仍为方案 C 的 P4 范畴，UI 预留位置即可）。
- 不改深层对话调度逻辑，只加展示与开关。
- 不为 P3 反思压缩做完整 UI，但记忆管理中心的分层/摘要视图已为其预留结构。
