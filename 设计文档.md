# 伪春菜复兴计划 — 设计文档（架构）

Electron 桌面悬浮桌宠「小未来」：透明置顶、可拖动、不占任务栏。对话、开场白、点击回应、主动搭话全部由本地/局域网大模型（OpenAI 兼容 `/v1`）现场生成，具备长期记忆、日程提醒、情感状态与可编辑人格。

---

## 1. 技术栈

- **Electron 43.3.0**（跨平台桌面壳）
- 前端：纯 HTML / CSS / JavaScript（无框架、无构建步骤）
- 后端：Node.js CommonJS
- 对话：OpenAI 兼容 `/v1/models` + `/v1/chat/completions`（Ollama / vLLM / LM Studio）
- 存储：`src/services/json-storage.js` 封装的 JSON 文件（按 schemaVersion 迁移）

---

## 2. 目录结构（现状）

```
.
├── AGENTS.md                 开发代理必读（坑、架构、命令）
├── README.md                 用户指南
├── package.json              入口、脚本（start/dev/check）
├── assets/character/         idle.png / happy.png / sad.png（1254×1254）
├── src/
│   ├── main/
│   │   ├── main.js           主进程：窗口、IPC 注册、对话调度、各服务协调
│   │   ├── preload.js        安全桥接：contextBridge 暴露 desktopPet.*
│   │   └── ipc-validation.js 每个 IPC 通道的入参校验（统一错误结构）
│   ├── renderer/
│   │   ├── index.html / style.css / renderer.js   角色主窗口（气泡、拖拽、菜单触发）
│   │   ├── chat-input.html / chat-input.js        对话输入框（独立窗口）
│   │   ├── menu.html / menu.js                    右键 HTML 菜单
│   │   ├── settings-panel.*   设置与记忆
│   │   ├── provider-panel.*   Provider 设置
│   │   ├── schedule-panel.*   日程提醒
│   │   ├── owner-panel.*      关于主人
│   │   └── personality-panel.* 小未来的性格
│   ├── engine/
│   │   ├── generic.js         OpenAI 兼容 LLM 调用器（generateReply / generatePetLine / 流式）
│   │   ├── rules.js           仅暴露 loadConfig / resetConfig（读取合并后人格，供 generic 使用）
│   │   └── ollama.js          旧版 Ollama 专用实现（保留但主流程不用）
│   ├── services/              领域服务（见 §3.2）
│   └── core/                  出厂配置与状态核心
│       ├── personality.json   人格出厂默认（只读）
│       ├── llm-providers.json Provider 唯一配置源
│       ├── emotion-state.js   情感 reducer + 时间衰减 + 事件日志
│       ├── state-store.js     情感持久化
│       └── mood-map.js        情感 → 表情映射
└── test/                      node:test 自动化测试（npm run check）
```

---

## 3. 架构分层与数据流

### 3.1 进程边界

```
渲染进程 (renderer)        安全桥接 (preload)          主进程 (main)
─────────────────          ────────────────          ────────────────
角色UI / 气泡 / 菜单  ─desktopPet.*──► contextBridge  ─IPC─► ipcMain.handle
各面板窗口                     │
                              └─ 仅白名单 API，nodeIntegration 关闭
```

- **渲染进程**：只做 UI 与交互，不直接访问文件系统/Node，所有能力经 `desktopPet.*`。
- **主进程**：窗口管理、IPC 校验、对话调度、服务协调（情感/记忆/日程/主动/Provider）。

### 3.2 领域服务（services/）

| 服务 | 职责 | 持久化 |
|---|---|---|
| `json-storage` | JSON 文件读写 + schema 版本迁移 + 损坏恢复 | — |
| `memory-service` / `memory-store` | 长期记忆增删查、重要性/置信度、归档、prompt 注入 | `memory.json` |
| `memory-intent` | 从一句话解析记忆意图（记住/忘记/回查/不要记） | — |
| `scheduler-service` | 日程增删改、到期触发、重复规则 | `schedules.json` |
| `settings-store` | 应用开关（通知/声音/动画/减少动态/网络记忆同意等） | `settings.json` |
| `proactive-settings-store` + `proactive-policy` | 主动搭话的开关/安静时段/频率预算/冷却 | `proactive-settings.json` |
| `owner-store` | 主人称呼与资料 | `owner.json` |
| `personality-runtime` | 人格运行时覆盖，深合并到出厂默认 | `personality-runtime.json` |
| `conversation-orchestrator` | 对话编排薄封装（send/sendStream） | — |
| `reminder-gate` | 提醒的允许时段门控 | — |

### 3.3 对话调度（main.js 的 `sendChat` / `sendChatStream`）

```
用户输入
  → 1. 主人自报称呼？  extractOwnerName("我叫X") → 写入 owner.json，返回温情回复
  → 2. 记忆意图？      parseMemoryIntent(记住/忘记/回查) → memory 服务处理
  → 3. 其余全部走大模型 generic.generateReply()
        · 按 providerChain() 顺序探测，自动回退到第一个可用 provider
        · systemPrompt = {personality} 注入 + 主人称呼铁律 + 情感状态 + 相关记忆
  → 4. 全失败 → 兜底回复
```

对话使用 **sses 流式**（`chat:sendStream` / `chat:submit`），增量块经 `chat:delta` 推给渲染进程逐字显示；同时保留最近 12 轮进程内历史。

### 3.4 主动搭话（proactive）

```
渲染层空闲定时（20~60s）→ proactive:decide
  → 尝试用 LLM generatePetLine(purpose='idle') 现场生成一句搭话
  → proactivePolicy.decide：开关 / 安静时段 / 每小时-每日预算 / 冷却 / 查重
  → 通过 → 气泡显示；LLM 不可用 → 不弹（无本地预设台词）
```

点击回应（`character:greet`）与开场白（`character:greeting`）同样走 `generatePetLine`（purpose 分别为 `click` / `greeting`），全部 LLM 生成。

---

## 4. 配置要点（勿硬编码进代码）

### `src/core/llm-providers.json`
- `activeProvider`：当前激活 provider 名（菜单「Provider 设置」可改并持久化）。
- `providers.*`：每个 provider 为 OpenAI 兼容配置
  - `baseUrl`（含 `/v1`）、`apiKey`（`none`/`EMPTY` 时不加 Authorization）、`defaultModel`、`temperature`、`topP`
- provider 键的**书写顺序**即自动回退顺序（active 优先）。

### `src/core/personality.json`
- 角色出厂人格（只读默认）。`systemPrompt` 中 `{personality}` 被替换成整个 personality 对象注入。
- `personality` 子对象：`mood / age / likes / dislikes / catchphrases / tone / selfIntro`。
- 用户编辑存 `personality-runtime.json`（深合并：对象逐键，数组/标量整体覆盖）；升级不覆盖。

### 用户数据目录
`~/Library/Application Support/haruhana-quest/`：`memory.json`、`schedules.json`、`settings.json`、`proactive-settings.json`、`emotion-state.json`、`owner.json`、`personality-runtime.json`。

---

## 5. IPC 接口（preload 暴露 `window.desktopPet`）

### 角色与对话

| 方法 | 通道 | 说明 |
|---|---|---|
| `greet()` | `character:greet` | 点击回应（LLM 生成） |
| `greeting()` | `character:greeting` | 开场白（LLM 生成，返回 `{line, known}`） |
| `sendChat(input)` | `chat:send` | 非流式完整调度 |
| `sendChatStream(input)` | `chat:sendStream` | 流式调度 |
| `chatSubmit(input)` | `chat:submit` | 输入框提交（流式，配合 onChatDelta） |
| `onChatDelta(cb)` | `chat:delta` | 流式增量推送 |
| `openChatInput/closeChatInput/resizeChatInput` | `chat:*` | 输入框窗口控制 |

### 记忆 / 设置 / 日程 / 主动

| 方法 | 通道 |
|---|---|
| `memory.list/remember/update/remove/forget/doNotRemember/archive/archiveExpired/export/clearAll` | `memory:*` |
| `settings.get/set` | `settings:*` |
| `schedule.list/create/update/remove/clear` | `schedule:*`（另开面板） |
| `proactive.get/set/pause/resume/decide` | `proactive:*` |
| `onReminder(cb)` / `onProactiveDecision(cb)` / `onToast(cb)` | 推送 |

### 主人 / 人格 / Provider / 菜单

| 方法 | 通道 |
|---|---|
| `owner.get/set` + `openOwnerPanel/closeOwnerPanel` | `owner:*` |
| `personality.get/set/reset` + `openPersonalityPanel/closePersonalityPanel` | `personality:*` |
| `getProviderConfig/saveProviderConfig/checkProviderApi` + 面板开关 | `provider:*` |
| `getMenuData/setProvider/resetPosition/hide/quit/clearMemory/showState` + 菜单开关 | `menu:*` |
| `getState/getMoodMap/onStateChanged/onShowState` | `state:*` / `show:state` |
| `getScreenWorkArea/moveBy` | `screen:*` / `window:*` |

### 校验与统一错误

- 所有增删改类 IPC 经 `guarded()` 包装，用 `ipc-validation.js` 校验类型/长度/枚举。
- 非法入参返回统一错误：

```json
{ "ok": false, "error": { "code": "INVALID_PAYLOAD", "message": "请求参数不合法或超出允许范围", "recoverable": true } }
```

---

## 6. 情感状态

`src/core/emotion-state.js`：

- **六维状态**：`mood / affection / energy / health / stress / loneliness`（0～100）。
- **reducer**：所有变更走 `recordEvent(type, delta)`，内部 `boundedDelta` 限制单次与每日增幅，防刷分。
- **时间衰减**：按最后更新时间 `applyDecay`，长期不启动缓慢回归，不归零。
- **事件日志**：`events` 数组保留最近若干条（含 type、delta、reason、createdAt）。
- **注入 LLM**：`generateReply`/`generatePetLine` 通过 `buildBasePrompt` 把当前情感状态注入 system prompt（仅调语气，不暴露原始数值；不影响任何权限）。
- **表情映射**：`mood-map.js` 把 mood → idle/happy/sad 图片。

---

## 7. 已实现功能（摘要）

- 透明悬浮置顶角色，拖动 / 单击 / 双击交互，气泡 + 表情。
- 对话/开场/点击/主动全走大模型，无本地预设台词。
- 主人称呼识别（我叫X）、记忆指令、长期记忆注入。
- 日程与到期提醒（含安静时段与重复规则）。
- Provider 多后端、自动回退、流式输出、配置持久化。
- 情感状态 + 状态面板 + 时间衰减。
- 五个设置/信息面板：Provider、设置与记忆、日程、关于主人、性格。
- 可编辑人格（运行时覆盖 + 恢复出厂）。

---

## 8. 已知遗留 / 待办

- `src/engine/ollama.js` 为旧版 Ollama 专用实现，主流程未用（仅测试引用），可删或并入 generic。
- 长期记忆为**关键词/类型/重要性检索**，无向量检索、无自动保存每句、无复杂知识图谱。
- 无 Agent 工具系统 / 审批流 / 安全扫描 / 打包分发（见 `小未来桌面管家-SPEC.md` 路线）。
- 菜单、面板均为独立窗口 + 自定义 HTML，未做统一的 IPC 拆分目录（`main/ipc*.js`）。
