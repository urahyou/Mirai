# 小未来桌宠功能文档

> 本文档描述**当前已实现**的功能与已知未实现项，作为验收与回归依据。
> 架构与开发细节见《设计文档.md》，产品蓝图见《小未来桌面管家-SPEC.md》。

## 1. 项目概述

一个基于 Electron 43 的**透明桌面桌宠**「小未来」。角色悬浮在桌面，可以拖动、点击、双击聊天。对话、开场白、点击回应、主动搭话全部由**本地或局域网大模型**现场生成（OpenAI 兼容 `/v1` 接口），不包含任何本地预设台词。

当前技术栈：

- Electron 43.3.0 + 原生 HTML/CSS/JavaScript（CommonJS）
- 对话后端：Ollama / vLLM / LM Studio（OpenAI 兼容）
- 存储：JSON 文件（`json-storage.js`，带 schema 版本迁移）
- 测试：node:test，`npm run check` 执行

## 2. 已实现功能

### 2.1 桌宠窗口

- 透明窗口、无边框、始终置顶（screen-saver 层级）、不占任务栏
- 默认定位屏幕右下角，窗口 `320×360`
- 支持重置位置、隐藏角色（菜单）、退出应用
- 对应文件：`src/main/main.js`

### 2.2 角色交互

| 操作 | 效果 |
|---|---|
| 单击 | 大模型生成一句俏皮回应（`character:greet`） |
| 双击 | 打开独立对话输入框（可拖动、回车发送 / Esc 关闭 / Shift+Enter 换行） |
| 按住拖拽 | 移动桌宠窗口；拖动后不误触发单击 |
| 右键 | 打开自定义 HTML 菜单 |
| 首次启动 | 大模型开场白，未认识主人时顺势询问称呼 |

对应文件：`src/renderer/renderer.js`、`src/renderer/chat-input.*`、`src/renderer/menu.*`

### 2.3 对话气泡与表情

- 气泡自动出现/消失，回复越长显示越久
- 支持多行与长文本换行
- 说话时轻微动画
- 三表情按内容自动切换：idle / happy / sad（`assets/character/*.png`）

### 2.4 大模型对话（核心）

- 调用链：`chat:submit` → `sendChatStream` → `generic.generateReply`
- 流程：
  1. 主人称呼识别（`extractOwnerName`）
  2. 记忆意图处理（`parseMemoryIntent`）
  3. 按 provider 优先级自动回退，请求 `/v1/chat/completions`
  4. 全失败 → 兜底回复
- 上下文组装顺序：人格 systemPrompt → 主人称呼铁律 → 情感状态 → 相关长期记忆 → 最近 12 轮历史 → 当前输入
- **流式输出**：SSE 增量经 `chat:delta` 逐字显示
- 所有对话、开场白、点击回应、主动搭话都是 LLM 现场生成（**无本地预设台词**）

### 2.5 主人称谓（owner）

- 识别「我叫X / 我的名字是X / 请叫我X / 以后叫我X / 记住叫我X」等句式
- 写入 `owner.json`，之后开场白、点击、主动搭话、对话回复都用该称呼
- 菜单「关于主人」面板可编辑称呼、生日、喜好、备注

### 2.6 长期记忆（memory）

- 意图指令：记住 / 忘记 / 回查 / 不要记住这类
- 记忆条目：id、type、content、importance、confidence、source、sensitivity、expiresAt 等
- 记忆类型：profile / preference / episodic / relationship / work / schedule
- 跨重启持久化（`memory.json`），检索注入对话上下文（标明"可能相关的历史信息"）
- 记忆面板：列表、按类型过滤、单条删除、导出、清空全部

### 2.7 日程与提醒（schedule）

- 自然语言/面板创建日程，到期触发（`reminder:fire` 推给渲染进程显示气泡）
- 支持类型（reminder/deadline/proactive）、重复规则（daily/weekly）、启用/暂停/删除/清空
- 安静时段门控（`reminder-gate`），跨重启持久化（`schedules.json`）
- 日程面板：今日提醒、新建/编辑/暂停/删除

### 2.8 Provider 管理

- 配置文件 `src/core/llm-providers.json`（唯一配置源）
- 多 Provider、自动回退链、`/models` 可用性探测
- 菜单「选择 Provider」快速切换；「Provider 设置」面板可编辑地址/模型/参数并**持久化**
- `apiKey` 为 `none`/`EMPTY` 时不加 Authorization 头

### 2.9 角色人格（personality）

- 出厂人格 `personality.json`（只读默认）+ 运行时覆盖 `personality-runtime.json`（深合并）
- 可编辑：名字、心情、年龄、喜好、讨厌、口头禅、语气、自我介绍
- 菜单「小未来的性格」面板：编辑/保存/恢复默认，保存后**立即生效**（无需重启）
- `{personality}` 注入 systemPrompt

### 2.10 情感状态（emotion）

- 六维：mood / affection / energy / health / stress / loneliness（0~100）
- 互动触发变化（greeting / praise / negative / chat / app_started 等），带单次与每日增幅上限
- 时间衰减、跨重启持久化（`emotion-state.json`）
- 情感状态注入 LLM（只调语气），并映射角色表情
- 状态面板（菜单「查看状态」）：显示当前值、原因、可拖动

### 2.11 主动搭话（proactive）

- 渲染层空闲定时（20~60 秒）触发 `proactive:decide`
- **内容由 LLM 现场生成**（`generatePetLine` purpose=idle）
- 策略门控：开关、暂停、安静时段、每小时/每日预算、冷却、内容查重
- LLM 不可用时静默（不弹预设台词）

### 2.12 安全通信

- `nodeIntegration` 关闭、`contextIsolation` 开启
- 渲染进程仅能调用 `window.desktopPet` 白名单 API
- 所有 IPC 入参经 `ipc-validation.js` 校验，返回统一错误结构
- Provider API Key 不写入渲染进程

## 3. 未实现 / 待办

- **Agent 工具系统**：无只读系统工具（getSystemStats / disk / process / file read 等）
- **任务与审批流**：无任务面板、计划展示、L2/L3 权限确认
- **安全扫描与隔离区**：无
- **系统事件触发**：无开机问候、鼠标闲置、日期/久坐/深夜/盘满等主动提醒
- **更多动画与表情**：仅 idle/happy/sad 静态图，无 GIF/眨眼/呼吸/任务动画、无 excited/tired/love/working 等状态
- **成长与纪念日**：无每日总结、纪念日、多角色/皮肤包体系
- **打包发布**：无 .dmg/.exe 打包、自动启动、应用图标安装、自动更新
- **数据库化**：当前为 JSON 文件存储，无 SQLite、无 migration 框架、无崩溃恢复备份

## 4. 文件对应关系

| 文件 | 作用 |
|---|---|
| `src/main/main.js` | 主进程：窗口、IPC 注册、对话调度、服务协调 |
| `src/main/preload.js` | 安全桥接（desktopPet.\*） |
| `src/main/ipc-validation.js` | IPC 入参校验 |
| `src/renderer/renderer.js` | 角色交互、气泡、菜单触发、开场白 |
| `src/renderer/chat-input.*` | 对话输入框 |
| `src/renderer/menu.*` | 右键 HTML 菜单 |
| `src/renderer/{settings,provider,schedule,owner,personality}-panel.*` | 各设置面板 |
| `src/engine/generic.js` | OpenAI 兼容 LLM 调用器（对话/主动/流式） |
| `src/engine/rules.js` | 合并人格读取（loadConfig/resetConfig） |
| `src/engine/ollama.js` | 旧版 Ollama 调用（主流程未用） |
| `src/services/*` | 记忆、日程、设置、主动、主人、人格运行等 |
| `src/core/emotion-state.js` | 情感 reducer / 衰减 / 事件日志 |
| `src/core/mood-map.js` | 情感 → 表情映射 |
| `src/core/personality.json` | 人格出厂默认 |
| `src/core/llm-providers.json` | Provider 唯一配置源 |
| `assets/character/` | 表情图 idle/happy/sad |
| `test/` | 自动化测试（`npm run check`） |

## 5. 验证方式

```bash
npm run check   # 语法检查 + 48 个自动化测试
npm start       # 启动
npm run dev     # 开发模式（DevTools + 渲染日志）
```

无 lint / typecheck。应用层错误观察 `/tmp/pet_*.log`。
