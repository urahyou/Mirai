# 小未来当前设计

本文描述仓库当前实现，不描述已经删除的记忆、日程、工具或主动打扰模块。

## 产品边界

小未来是一个透明悬浮的 Electron 桌宠，核心体验只有四部分：

1. Live2D 角色展示与轮廓命中交互。
2. 点击角色后的单句互动回复。
3. 轻量输入框和可展开的聊天记录窗口。
4. Provider 与人格配置。

角色窗口默认驻留右下角、无边框、不占任务栏。桌宠窗口可保持置顶；展开聊天记录时，角色和聊天窗口暂时回到普通窗口层级，以便被其他应用覆盖。显示设置还可以给 Live2D Canvas 轮廓增加柔和外阴影，设置实时同步到角色窗口。

## 窗口与渲染

```text
mainWindow
└── renderer/index.html
    ├── Live2D Canvas
    ├── 角色气泡
    └── renderer.js

chatInputWindow
└── renderer/chat-input.html
    ├── 轻量输入框
    ├── 展开聊天记录
    └── chat-input.js
```

角色完全由 `assets/live2d/models/.../model3.json` 渲染。旧的角色 PNG fallback 已删除。所有角色点击、拖拽和右键操作都经过 `Live2DAvatar.isHit()`；它同时检查模型 hit area 和模型实际轮廓。

## 对话流程

### 点击互动

角色轮廓单击经过双击判定和冷却后调用 `character:greet`。主进程按 Provider 顺序探测可用服务，调用 `generic.generatePetLine()` 生成一条不进入正式上下文的回复。回复写入 `chat-history.json`，并显示在角色气泡和聊天记录中。

### 正式聊天

```text
输入框提交
  → chat:submit
  → 写入 user 消息
  → chat:delta(started)
  → providerChain() 探测并回退
  → generic.generateReply() 流式读取 SSE
  → chat:delta(每个增量)
  → 写入 assistant 消息
  → chat:delta(done)
```

`generic.js` 只保留当前 Electron 进程中的最近 12 轮上下文；它与用于展示的持久聊天记录是两个不同层次。重新启动应用后，聊天记录仍可查看，但不会自动作为模型上下文全部注入。

气泡层使用 turnId 管理当前会话：正式聊天会淘汰旧的点击互动，过期回复不能覆盖新回复。连续点击有冷却时间，流式回复会显示输入中动画并自动滚动到最新内容。

## 安全边界

- 渲染进程不直接访问 Node.js，只使用 `window.desktopPet`。
- `contextIsolation` 保持开启，`nodeIntegration` 保持关闭。
- IPC 入参在主进程校验。
- Provider 地址和密钥来自配置文件，不写入代码。
- 应用没有工具执行、命令执行、上传或云端同步能力。

## 配置与持久化

| 数据 | 来源或文件 |
| --- | --- |
| 出厂人格 | `src/core/personality.json` |
| Provider | `src/core/llm-providers.json` |
| 用户人格覆盖 | `userData/personality-runtime.json` |
| 显示设置 | `userData/display-settings.json` |
| 聊天记录 | `userData/chat-history.json` |

## 验证

```bash
npm run check
```

验证包括主进程、预加载、渲染脚本语法检查，Provider 流式行为、人格与显示设置、聊天历史、IPC 契约以及 Live2D 模型资源检查。

## 后续方向

- 更丰富的 Live2D motion 和表情参数映射。
- 聊天窗口的新消息提示、未读状态和历史搜索。
- 打包分发、自动更新和多模型皮肤包。

记忆库、日程、主动打扰和 Agent 工具不属于当前版本范围，除非重新设计产品边界，否则不要恢复对应模块。
